[project]
name = "uk-rail-aggregate"
version = "0.1.0"
description = "Builds the passenger-hours dashboard JSON from Athena"
requires-python = ">=3.10"
dependencies = ["boto3>=1.34"]

[tool.ruff]
line-length = 100
